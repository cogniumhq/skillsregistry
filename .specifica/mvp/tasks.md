# SkillsRegistry Local — MVP Tasks

**Open work.** Discrete items, status, notes. Long narrative lives in `design.md`.

Status legend: `☐` open · `▶` in progress · `☑` done · `⊘` cancelled · `⏸` blocked

Ordered roughly by dependency, not by priority. Phase-1 tasks (SDK extraction) must complete before phase-2 (local app skeleton). Phase-3 is release + docs.

---

## Phase 0 — Repo scaffold

- ☑ **T-0.1** Create directory structure, LICENSE (Apache-2.0), NOTICE, .gitignore, README stub, CLAUDE.md
- ☑ **T-0.2** Write `.specifica/principles.md`, `spec.md`, `design.md`, `tasks.md`
- ☑ **T-0.3** Add `package.json` (workspace root), `pnpm-workspace.yaml`, `tsconfig.base.json`
- ☑ **T-0.4** Add `.changeset/config.json` with `access: "public"` for `@skillsregistry/*`
- ☑ **T-0.5** Add CI workflows: `.github/workflows/ci.yml`, `publish-sdk.yml`, `publish-app.yml`
- ☑ **T-0.6** Add `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `SECURITY.md` (security@cognium.net + 90-day embargo)
- ☐ **T-0.7** Configure branch protection on `main` (require CI green, require review) — repo-admin task, not code
- ☐ **T-0.8** Create npm org / scope registration for `@skillsregistry/*` — publish permissions

## Phase 1 — SDK extraction from mothership

Each extraction lands in this repo, publishes `1.0.0` to npm, then the mothership repo consumes it in a follow-up PR (tracked in mothership's own `tasks.md`, not here). Mothership eval must pass after each step.

- ☑ **T-1.1** Extract `@skillsregistry/schema` — moved `src/db/schema.ts` + all 32 `src/db/migrations/*.sql` from mothership into `packages/schema/`. Added runtime-agnostic migration runner (`runMigrations`, `assertSchemaAtLeast`, `loadBundledMigrations`) plus `SCHEMA_VERSION = 32` constant. Build + typecheck pass. Changeset queued for `1.0.0` npm publish.
- ☑ **T-1.2** Extract `@skillsregistry/contracts` — moved `src/schemas/{common,responses}.ts` from mothership. Added new `./upstream` entry point with `TrustScoreRequest`/`Response`, `BudgetResponse`, `PublishRequest`/`Response`, `TrustScoreDelta`/`SyncResponse`, and a 9-code `UpstreamErrorCode` taxonomy. Build + typecheck pass. Changeset queued for `1.0.0`.
- ☑ **T-1.3** Move `@skillsregistry/dag` from `mothership/packages/dag/` into `packages/dag/` here. License normalized MIT → Apache-2.0 (never published under MIT). All 74 tests pass. Build + typecheck pass. Changeset queued for `1.0.0` publish. Closes mothership pending workstream #4 (SDK publishing decision).
- ☐ **T-1.4** Extract `@skillsregistry/domain` — landed in six sub-PRs. Only publish `1.0.0` after (f) so consumers see one coherent surface.
  - ☑ **T-1.4a** Adapter interfaces skeleton — `packages/domain@0.1.0` scaffolded with all five ports (`KvAdapter`, `QueueAdapter<T>`, `ArtifactAdapter`, `EmbedderAdapter`, `AfterResponse`) at `src/adapters/`. `EmbedderIdentity` surfaces model identity for cross-model write safety. Nothing moved yet. Build + typecheck pass.
  - ☑ **T-1.4b** Ported `src/resilience/*` at `packages/domain/src/resilience/`. `CircuitBreaker` verbatim (no env dep) with its 11-test suite green. `TenantCircuitRegistry` refactored to options-only constructor (dropped `Env`). `LLMProxyRateLimiter` rewritten against `KvAdapter` + `AfterResponse` — degrades open when no KV adapter; key namespace preserved wire-compatible. Exposed via package root and `@skillsregistry/domain/resilience` subpath. Bumped to `0.2.0`.
  - ☑ **T-1.4c** Ported `src/providers/*` at `packages/domain/src/providers/`. `SearchProvider` interface verbatim. `PgVectorProvider` refactored to `PgVectorProviderOptions { pool: SqlPool, ...tuningKnobs }` — dropped `Env`/`createPool`, exposed all 10 tuning knobs as typed options with mothership defaults preserved. Introduced `SqlPool` / `SqlConnection` / `SqlClient` / `SqlQueryResult` ports at `adapters/sql.ts` (structurally pg-compatible). Added search-facing types at `src/types.ts` (`SkillInput`, `EmbeddingSet`, `SearchFilters`, `SearchOptions`, `SearchResult`, `ScoredSkill`, `ConfidenceSignal`, `SearchMeta`, plus status/tier/badge unions). Ported `textNormSha256` at `ingestion/text-fingerprint.ts`. Exposed via package root, `@skillsregistry/domain/providers`, and `@skillsregistry/domain/types`. Bumped to `0.3.0`.
  - ☑ **T-1.4d** Ported `src/intelligence/*` at `packages/domain/src/intelligence/`. `ConfidenceGate` refactored to `ConfidenceGateOptions { provider, embedFn, cache, logger, pool, deepSearch, compositionDetector, reranker, ...tuningKnobs }` — mode-aware t1/t2 thresholds preserved, `AfterResponse.run(...)` replaces `ctx.waitUntil(...)`. `DeepSearch`, `CompositionDetector` moved onto `LlmAdapter.complete`; `Reranker` moved onto `SqlPool` + `RerankerBackend`. `LiteLLMRerankBackend` (Cohere-shape HTTP) shipped in the domain; Workers AI variant + factory stay in mothership (need CF binding). New ports at `adapters/`: `LlmAdapter`, `SearchCachePort`, `SearchLoggerPort`. New types at `types.ts`: `Appetite` + helpers, `FindSkillRequest`, `FindSkillResponse`, `SkillResult`, `CompositionResult`, `SearchLogEntry`. Relaxed `exactOptionalPropertyTypes` in domain tsconfig to match mothership's `strict: true` ergonomics (rationale documented inline). Exposed via `@skillsregistry/domain/intelligence` subpath. Bumped to `0.4.0`.
  - ☑ **T-1.4e** Ported `src/composition/*` at `packages/domain/src/composition/`. `forkSkill`, `copySkill`, `createComposition`, `extendComposition`, `publishComposition`, `getAncestry`, `getForks`, `getDependents`, `getCompositionBySlug` — every write-side function now takes a `CompositionAdapters { pool, embedQueue, scanQueue }` bundle instead of `Env`. Both queues consumed via the existing `QueueAdapter<T>` port with typed payloads (`EmbedQueueMessage`, `CogniumScanQueueMessage`). Zod input validators (`forkInputSchema`, `copyInputSchema`, `compositionInputSchema`, `extendInputSchema`) ported verbatim. Typed errors (`NotFoundError`, `ValidationError`) centralized at `composition/errors.ts`. `getCompositionBySlug` accepts an optional `shareUrlHost` so non-mothership consumers can override the `https://skillsregistry.net` default. New deps: `nanoid ^5.1.6`, `zod ^3.23.0` (direct — was already transitive). Exposed via `@skillsregistry/domain/composition` subpath. Bumped to `0.6.0`.
  - ☑ **T-1.4f** Ported `src/cognium/scoring-policy.ts` at `packages/domain/src/scoring/policy.ts`. Delivered ahead of T-1.4e because composition (fork) needs `BASE_TRUST` for the source-provenance floor. `BASE_TRUST` + `MAX_TRUST` tables verbatim; `computeTrustScore`, `capTrustBySource`, `deriveStatus`, `deriveTier`, `clampTrust`, `buildRemediationMessage` verbatim. D2 signature helpers refactored to take a plain numeric `penalty` instead of `Env` — mothership computes it once at boot via the exported `parsePositiveFloat(env.TRUST_SIGNATURE_REVOKED_PENALTY_POINTS, DEFAULT_SIGNATURE_REVOKED_PENALTY)` and passes the number to `applyRevocationScoring(pool, keyId, penalty)`. New domain types at `types.ts`: `CircleIRAnalysisPhase`, `ScanFinding`, `SkillRow`. Exposed via `@skillsregistry/domain/scoring` subpath. Bumped to `0.5.0`.
- ☑ **T-1.5** Ported `src/mcp/*` at `packages/mcp/`. Framework-agnostic JSON-RPC 2.0 dispatch (`handleMcpRequest`) returning `{ kind: 'json' | 'accepted' | 'error' }` so consumers wrap in any HTTP framework — no Hono / Workers coupling in the SDK. Five read-only v1 tools (`search_skills`, `get_skill`, `list_leaderboard`, `get_trust_breakdown`, `resolve_composition`) with input schemas ported verbatim. Discovery descriptor (`buildDiscoveryDescriptor`) for `/.well-known/mcp.json` + `/mcp.json` — prefers `config.canonicalOrigin` over the request URL. SqlPool-backed invocation recorder (`createSqlPoolInvocationRecorder`) for J8 observability (writes `mcp_invocations` + bumps `agent_invocation_count` for skill-resolving tools). Adapters bundle (`McpAdapters { search, skills, compositions, leaderboards, recorder?, afterResponse? }`) plugs consumer's existing services in as function pointers — no reflection, no runtime coupling to a specific DB driver. `search.findSkill` delegates to `@skillsregistry/domain`'s `ConfidenceGate` at the consumer boundary; consumer binds `AfterResponse` inside the port. Every env-configurable knob in mothership (`MCP_SEARCH_DEFAULT_LIMIT`, `MCP_SEARCH_MAX_LIMIT`, `MCP_SEARCH_QUERY_MAX`, `MCP_LEADERBOARD_DEFAULT_LIMIT`, `MCP_LEADERBOARD_MAX_LIMIT`, `MCP_BATCH_MAX`, `MCP_CANONICAL_ORIGIN`, `MCP_INVOCATION_ARGS_MAX`) is a typed field in `McpConfig` with documented defaults via `resolveConfig()`. Sub-paths: `.`, `./types`, `./discovery`, `./invocation-writer`. Test coverage: 14/14 green (dispatch happy + tool-domain-error + protocol-error paths, batch + notification handling, discovery descriptor shape + canonical-origin precedence). Peer dep on `@skillsregistry/domain` (workspace). Changeset queued for `1.0.0` first publish. Auth stance (verbatim from mothership): v1 `X-Tenant-Id` is an advisory scope hint (missing / spoofed = public-only); OAuth 2.1 + RFC 8707 land with v2 write tools.
- ☑ **T-1.6** Ported `src/eval/*` + `scripts/run-eval.ts` at `packages/eval/`. `evalFixtures` (90+ fixtures across 5 phrasing patterns), `computeMetrics` / `buildEvalResult` / `formatMetrics` (Recall@1 / Recall@5 / MRR + per-tier accuracy + latency percentiles + LLM-fallback lift + match-source distribution + per-pattern breakdown), `runEvalSuite(endpoint, tenantId, options)` (programmatic entry) all ported verbatim. `RunEvalOptions` extends the mothership shape with `headers?: Record<string, string>` so callers can attach `Authorization` or `X-Tenant-Id` without leaking secrets into the URL — the T-1.6 gate criterion. `EvalFixture` + `EvalMetrics` + `EvalResult` moved out of mothership `src/types.ts` into the package (co-located with `fixtures.ts` / `metrics.ts`) so consumers only need `@skillsregistry/eval`. `skillsregistry-eval` CLI binary (`bin` in `package.json`) adds `--auth "Bearer …"` and `-H "Name: Value"` (repeatable) flags on top of the mothership `--endpoint` / `--tenant` / `--limit` / `--verbose` / `--show-failed` set. Exit codes preserved (`0` for R@5 ≥ 0.5, `1` for < 0.5 or fatal). Sub-paths: `.`, `./fixtures`, `./metrics`, `./runner`, `./cli`. Peer dep on `@skillsregistry/domain` (workspace) for `FindSkillRequest` + `FindSkillResponse`. Test coverage: 24/24 green (12 fixture validation + 12 metric compute/formatter). Changeset queued for `1.0.0` first publish. Fixtures dated at extraction (May 2026 mothership fixtures.ts, unchanged — dated in the T-1.7 migration PR to prove parity).
- ☐ **T-1.7** Mothership migration PR: consume all six packages from npm, delete inlined copies, run eval — R@5 ≥ 80% required to merge. (Landed in `cogniumhq/skillsregistry`, tracked here as a coordination dependency.)

## Phase 2 — Local app (`apps/local`)

- ☑ **T-2.1** Scaffolded `apps/local/` at `@skillsregistry/local@0.1.0`. `package.json` wires workspace deps on all six `@skillsregistry/*` packages plus Hono + `@hono/node-server` + `pg`. `tsconfig.json` extends the base with `exactOptionalPropertyTypes: false` + `noUncheckedIndexedAccess: false` for mothership-port parity. `src/index.ts` exports `createApp(config)` (Hono app with `GET /v1/health` for T-2.18 liveness) and a `main()` that loads config, mounts the app, and serves via `@hono/node-server` (`isMain` guard so tests import without booting). `Dockerfile` is a two-stage `node:22-alpine` build with pnpm 9.15.0, `pnpm deploy --prod /out` for a lean runtime image, non-root `app` user, and a `wget /v1/health` HEALTHCHECK. `docker-compose.yml` brings up `pgvector/pgvector:pg16` + `ollama/ollama:latest` + the app with sensible `depends_on: service_healthy` gating and env-file passthrough. `.env.example` documents every env var. `.dockerignore` keeps builds slim. Verification: `pnpm --filter @skillsregistry/local build` clean, typecheck clean, `node dist/index.js` boots and `curl /v1/health` returns `{status:"ok",...,upstreamConfigured:false,embedder:"ollama"}`.
- ☑ **T-2.2** Node adapters — five of six shipped at `apps/local/src/adapters/`. `upstream-embedder.ts` is *intentionally deferred* per the T-2.7 note: `@skillsregistry/contracts/upstream.ts` has no `/v1/embed` endpoint to build against, so shipping the adapter first requires a mothership contract change (tracked as a coordination ask on T-1.7, not blocking Phase 2 completion). Ollama is the only working embedder for MVP. `pg-kv.ts` exposes `PgKv` + `ensureKvStoreTable(pool)` + the `PG_KV_TABLE_DDL` constant; the DDL lives here (not in `@skillsregistry/schema`) because `kv_store` is a local-node concern — the mothership binds `KvAdapter` to Cloudflare KV natively. Lazy TTL eviction on read (no background sweep) matches the CF KV semantics the domain layer expects. `memory-queue.ts` implements `QueueAdapter<T>` over an in-process array with a single-handler consumer contract (`onMessage(handler)`); messages sent before a handler is bound are buffered and drain on next microtask. Handler rejections route through an injectable `onError` (defaults to `console.error`), never rethrown to the producer. Named `memory-queue.ts` rather than `pg-queue.ts` for accuracy — the Postgres LISTEN/NOTIFY variant lands post-MVP. `fs-artifact.ts` implements `ArtifactAdapter` under `<baseDir>/<key>`; enforces absolute `baseDir` at construction and rejects empty keys, absolute keys, and `..`-traversal in `key`. Uses `fs/promises` (`mkdir` recursive on write, `null` on `ENOENT` read, idempotent delete). `ollama-embedder.ts` ships an async factory `createOllamaEmbedder(options)` that probes the model's output dim with a single `"."` embed before constructing the `EmbedderAdapter` — `EmbedderIdentity.id` defaults to `<model>@ollama-<dim>` (overridable via `identityOverride`) so cross-model write safety works without a manifest lookup. Uses the batch `POST /api/embed` endpoint (`{ embeddings: number[][] }`) with a legacy `{ embedding: number[] }` fallback. Every returned row is validated against the probed dim; mismatch throws. `node-after-response.ts` implements `AfterResponse` via `setImmediate` so the HTTP socket has flushed before the background task runs — task rejections route through injectable `onError` (defaults to `console.error`), never rethrown. `adapters/index.ts` barrel re-exports the five adapters + their option types. Test coverage: 36/36 green — 6 queue, 8 fs, 4 after-response, 10 ollama-embedder (fake fetch queue), 8 pg-kv (in-memory FakePool speaking the exact SQL shapes). Total apps/local suite: 65/65 green. Typecheck + build clean. Real-DB pg-kv verification path is `docker compose up`.
- ☑ **T-2.3** Landed the composition root + route surface at `apps/local/src/{services.ts,middleware/*,routes/*,index.ts}`. `buildAppServices(config, pool)` is the single async factory that constructs the entire adapter graph — `PgKv` (+ `ensureKvStoreTable(pool)` at boot), `MemoryQueue<EmbedQueueMessage>`, `MemoryQueue<CogniumScanQueueMessage>`, `FsArtifact` (rooted at `config.artifact.baseDir`), `NodeAfterResponse`, `createOllamaEmbedder(...)` (async probe), and `UpstreamClient` (always instantiated; air-gap when `config.upstream === null`). `EmbedderConfig kind: 'upstream'` throws with a T-1.7 pointer until the mothership `/v1/embed` contract lands. `closeAppServices` is a no-op seam for future queue-drain. Concrete domain-service wiring (`SearchService`, `CompositionService`, etc.) is *intentionally deferred* to T-2.11/T-2.12/T-2.13 where each service's dependency shape is knowable — `ConfidenceGate` alone needs 10+ collaborators (embedder, reranker cache, log sink, per-tier thresholds), and pre-wiring blindly would either force placeholder stubs or lock the constructor shape ahead of the handler that consumes it. `middleware/admin-auth.ts` returns a Hono `MiddlewareHandler` from `adminAuth({ token })`: validates `Authorization: Bearer <token>` (case-insensitive scheme + header name per RFC 6750 spirit), uses `crypto.timingSafeEqual` on `Buffer` after length-check to avoid timing disclosure, throws at construction if token is empty, returns `401 { error: { code: 'unauthenticated', message } }`. `middleware/tenant.ts` reads `X-Tenant-Id`, trims, applies the shape guard `/^[A-Za-z0-9._:-]{1,128}$/`, stashes `string | null` on `c.set('tenantId', …)` via a typed `ContextVariableMap` module-augmentation so downstream handlers get typed access without casts. Invalid or missing values silently degrade to `null` (public visibility) per v1 MCP semantics — never 400'd, since `X-Tenant-Id` is advisory scope, not auth. `getTenantId(c)` is the read helper. Route surface split into three sub-apps: `createPublicRoutes(services)` mounts `tenantContext` + 501 stubs for `/search`, `/skills/:id`, `POST /skills`, `/leaderboards/:kind`, `POST /trust/score` (all tagged `task: 'T-2.11'`); `createAdminRoutes(services, adminToken)` scopes the `adminAuth` guard to `/admin/*` + `/migrate/*` (not `*` — sub-apps mounted at the same `/v1` prefix would otherwise leak the guard onto sibling public routes; the routing test caught this on first run) with 501 stubs for `/admin/budget`, `/admin/budget/refresh`, `/admin/health` (T-2.12), `/migrate/publish` (T-2.10); `createMcpRoutes(services, config)` mounts `tenantContext` + 501 stubs for `POST /mcp` (T-2.13), `GET /mcp.json` + `GET /.well-known/mcp.json` (T-2.14). Every stub returns `{ error: { code: 'not_implemented', message, task } }` so callers see which downstream task fills the handler in. `src/index.ts` `createApp(config, pool, services)` mounts admin first at `/v1` (path-specific `/admin/*` + `/migrate/*` win over any collisions), then public at `/v1`, then MCP at `/`. `main()` boot order is now `loadConfig → createPool → bootSchema → buildAppServices → createApp → serve`; shutdown handler drains services before the pool. Config gained `ArtifactConfig { baseDir: string }` + `ARTIFACT_BASE_DIR` env var (default `${cwd}/data/artifacts`, must be absolute — documented in `.env.example`). Test coverage: 33/33 new green — 7 admin-auth, 7 tenant, 19 route-surface smoke (health 200/503 by pool reachability + upstream-configured reporting, all 5 public 501 stubs, admin 401 without/wrong token + 501 with correct token, migration door 401 without token + 501 with T-2.10 marker, MCP 3 routes, 404 fallthrough). Route test injects `{} as AppServices` — safe because stub handlers `void services;` never dereference, so a full route-wiring smoke doesn't need per-adapter mocks. Total apps/local suite: **133/133 green**. Typecheck + build clean.
- ☑ **T-2.4** Landed `apps/local/src/config.ts`. `loadConfig(env = process.env): AppConfig` accumulates issues across the whole env and throws one `ConfigError` listing every problem (not first-failure) — so a fresh operator sees the full delta between their env and the required set in one shot. Required: `DATABASE_URL`, `ADMIN_TOKEN`. Defaults documented inline on every field (`HOST=0.0.0.0`, `PORT=3000`, `NODE_ENV=development`, `POSTGRES_POOL_MAX=10`, `POSTGRES_STATEMENT_TIMEOUT_MS=30000`, `LOG_LEVEL=info`). `EmbedderConfig` is a discriminated union (`{kind:'ollama',url,model}` \| `{kind:'upstream'}`) so downstream wiring is exhaustive. `UpstreamConfig` is null-when-air-gap-mode (all three of `MOTHERSHIP_URL`/`MOTHERSHIP_API_KEY`/`TENANT_ID` unset ⇒ null; any-but-not-all triggers an issue per missing var — partial config is always misconfig). `UPSTREAM_SEARCH_FALLBACK` defaults `true`. `CONFIG_ENV_VARS` exports the canonical name list for docs/tests. Test coverage: 21/21 green covering required, defaults, numeric parsing (bad PORT, sub-min timeout), embedder discrimination, upstream partial-config detection, boolean parsing, log-level whitelist, `NODE_ENV` whitelist, and the multi-issue accumulator behavior. Every documented env var also appears in `apps/local/.env.example`.
- ☑ **T-2.5** + **T-2.6** Landed together at `apps/local/src/boot/schema.ts`. One `bootSchema(client, options?)` entry point enforces the ordering — apply pending migrations, *then* assert `assertSchemaAtLeast(client, SCHEMA_VERSION)` — in a single call so no caller can skip the guard by accident. `options.applyMigrations = false` runs the guard alone (for setups where a DBA / init container owns migrations); `options.migrations` overrides the bundled set (tests, downstream extensions); `options.logger` swaps the sink (defaults to `console`). Each applied migration emits one info log with `{version}`; a "no pending migrations" info fires when everything's already recorded; boot start/complete lines carry `{applyMigrations, requiredVersion, currentVersion, appliedCount, skippedCount}` for structured tail-ability. Errors surface as-is from `@skillsregistry/schema` — migration failure aborts the boot with the file name + wrapped cause; version mismatch throws with `DB is at schema version X, SDK requires Y. Run migrations before starting.` `apps/local/src/db/pool.ts` adds a `createPool(config.postgres): pg.Pool` factory that applies `max` + `statement_timeout` from `PostgresConfig`. `src/index.ts` main() now boots in explicit order: `loadConfig → createPool → bootSchema → createApp → serve`, with a SIGINT/SIGTERM handler that closes the server + drains the pool. `GET /v1/health` gained a `dbReachable` field (pings `SELECT 1`; returns `503` + `status: degraded` on failure). Test coverage: 8/8 green covering fresh-DB apply-all, idempotency, `applyMigrations:false` happy path, `applyMigrations:false` version-mismatch, migration failure + ROLLBACK, guard-fires-on-partial-migrate, structured-log emission, and no-pending-migrations info. Tests use an in-memory `SqlClient` that speaks just enough Postgres (schema_migrations DDL, applied-rows SELECT, MAX(version), INSERT, BEGIN/COMMIT/ROLLBACK) to exercise the `@skillsregistry/schema` runner primitives end-to-end. Real-DB verification path is `docker compose up` (Postgres 16 + pgvector wired in `docker-compose.yml`).
- ☑ **T-2.7** Landed `apps/local/src/upstream-client/` — the single doorway from apps/local to `api.skillsregistry.net` (sacred-boundary invariant per `CLAUDE.md`). `UpstreamClient` exposes typed methods for every published mothership surface: `trustScore(POST /v1/trust/score)`, `getBudget(GET /v1/tenant/budget)`, `publish(POST /v1/publish)`, `getSkill(GET /v1/skills/:id)` (URL-encoded id, returns unknown for write-through cache callers), `getLeaderboard(GET /v1/leaderboards/:kind)` (URLSearchParams-built qs), `syncTrustScores(GET /v1/sync/trust-scores?since=…)`. Request bodies validated with the `@skillsregistry/contracts` Zod schemas before the wire; responses validated with the response schemas so contract drift fails loud at parse time (not at the field-access site three services down). Layered resilience per call, in this order: (1) air-gap gate — if `config === null`, every method throws `UpstreamError('upstream_not_configured')` before touching any state; (2) `TokenBucket` local-side rate limit (default 60 capacity / 1 rps → 60 rpm burst, env-configurable at construction) — throws `UpstreamError('rate_limited', retryAfter)`; (3) `CircuitBreaker` from `@skillsregistry/domain/resilience` (default threshold 3 / cooldown 30 s) — trips only on transport failures, timeouts, and 5xx (4xx client errors don't tally against breaker state — a 401 is a config problem, not a mothership-outage signal). When degraded, throws `UpstreamError('upstream_unavailable')`. `AbortError` from the AbortController timeout (default 30 000 ms) is preserved through the breaker via a captured-last-error slot and re-emerges as `UpstreamError('upstream_timeout')` instead of the generic unavailable. `UpstreamError` (in `errors.ts`) carries `code: UpstreamErrorCode` (from `@skillsregistry/contracts`), `retryAfter?`, `detail?`, `requestId?`, `cause?`. Response error decoding parses `UpstreamErrorSchema` first (structured envelope preferred — includes `request_id` for mothership-log cross-ref); falls back to `statusToCode(status)` inference (401→unauthenticated, 403→forbidden, 404→not_found, 429→rate_limited, other 4xx→bad_request). Fault-tolerant body parse — text() + JSON.parse in a try — so non-JSON error pages don't crash the mapper. Headers on every call: `Authorization: Bearer {apiKey}` + `X-Tenant-Id: {tenantId}` + `Accept/Content-Type: application/json`. Trailing slash in `baseUrl` is stripped. Test coverage: 35/35 green — 4 UpstreamError construction, 7 TokenBucket (invalid-config, initial-full, drain, time-based refill, cap-at-capacity, retry-after arithmetic, retry-after floor), 24 UpstreamClient (6 air-gap short-circuits, 5 happy-path methods with header/URL/query assertions, 6 error decoding via envelope + status fallback, 1 local rate-limit refusal, 3 breaker degradations via 5xx / ECONNRESET / AbortError, 4 input guards that never touch fetch, 1 baseUrl trailing-slash strip, 3 observability accessors). Total apps/local suite: 100/100 green. Typecheck + build clean. **Upstream-embedder deferral resolved:** the `EmbedderConfig kind: 'upstream'` branch in `config.ts` stays parseable but has no adapter yet — there's no `/v1/embed` endpoint in `@skillsregistry/contracts/upstream.ts` to build against. Adding one is a mothership contract change (coordination ask); until then, Ollama is the only working embedder. Tracked as a coordination note under T-1.7 rather than a local task.
- ☑ **T-2.8** Landed `apps/local/src/trust-client.ts` — the budget-aware wrapper between the T-2.11 `POST /v1/trust/score` handler and `UpstreamClient.trustScore(...)`. `TrustClient({ upstream, kv, pool, tenantId, logger? })` folds three concerns into one call site: (1) **precheck** — read `TRUST_BUDGET_KEY_PREFIX + tenantId` from KV (`trust:budget:v1:<tenantId>`), short-circuit with a locally-minted `UpstreamError('budget_exhausted', ..., { detail: { tokens_remaining: 0, tokens_reset_at, source: 'local_cache' } })` when `tokensRemaining <= 0`, so an exhausted tenant doesn't burn a mothership round-trip; missing / malformed / expired cache skips the check silently (T-2.9 may not be populating yet — the mothership is authoritative and will 402 on the actual call if we really are dry). (2) **upstream call** — `upstream.trustScore(request)`; any `UpstreamError` propagates unchanged so T-2.11 can surface `upstream_not_configured` → 503, `budget_exhausted` → 402, `rate_limited` → 429, etc. (3) **persist + budget** — on success, `UPDATE skills SET trust_score_v2, trust_tier, trust_results, trust_analyzed_at WHERE id = $5` best-effort (0-row match logs a warning but returns the score to the caller — the mothership is authoritative; the local row may not exist yet), then deduct `tokens_consumed` from the cached snapshot and re-write it with a refreshed `lowBalance` flag against the same 10% threshold the mothership uses. `BudgetSnapshot` (exported) is a subset of `BudgetResponse` in camelCase plus a `cachedAt` stamp — the payload shape shared between T-2.8 (reader) and T-2.9 (writer). `snapshotFromBudget(response)` is the one-place helper T-2.9 will call to derive the snapshot from a fresh `BudgetResponse`. `budgetKey(tenantId)` + `TRUST_BUDGET_KEY_PREFIX` exported so T-2.9 can't drift on key format. Air-gap semantics: `tenantId: null` is legal (route wiring stays uniform); `getCachedBudget()` returns null; `score()` re-throws whatever the underlying `upstream.trustScore()` throws (which is `UpstreamError('upstream_not_configured')`). Persistence failures (DB down, constraint violation) are logged as `error` and swallowed — the score is still returned so the API contract holds even if the local write fails. `TrustClientLogger` mirrors `BootLogger` shape (info/warn/error). Wired into `AppServices.trustClient` in `services.ts` (constructed with `tenantId: config.upstream?.tenantId ?? null`). Test coverage: 18/18 green — 1 key-format constant, 1 `snapshotFromBudget` mapping (with `vi.useFakeTimers` to freeze `cachedAt`), 6 `getCachedBudget` (null tenant, missing key, valid snapshot, JSON-parse failure, shape mismatch, KV throw), 5 happy-path `score` (upstream call + positional SQL args + budget decrement, 10% low-balance flip, clamp-at-zero when consumed exceeds cache, budget=null when no cache, budget=null in air-gap mode), 1 precheck short-circuit (upstream never called, KV untouched, error carries `source: 'local_cache'`), 2 persistence resilience (0-row match warns but returns; UPDATE throws → error logged, response still returned), 2 upstream error passthrough (`upstream_not_configured` from air-gap; `budget_exhausted` from mothership doesn't decrement the cache — T-2.9's meter will reconcile). Total apps/local suite: **151/151 green**. Typecheck + build clean.
- ☑ **T-2.9** Landed `apps/local/src/budget/meter.ts` — the nightly poller that keeps T-2.8's `TrustClient` precheck cache warm. `BudgetMeter({ upstream, kv, config, tenantId, scheduler?, logger? })` owns four concerns behind one class: (1) **cron scheduling** — `start()` runs one **boot refresh** immediately (so the KV cache is populated before the first `POST /v1/trust/score` if the process just came up) then schedules the recurring refresh via a pluggable `Scheduler` seam (default `NodeCronScheduler` wraps `node-cron`; tests inject a stub that captures the callback + exposes `trigger()` so the suite never touches wall-clock time); `stop()` tears the cron down for a clean shutdown; both are idempotent. (2) **refresh** — `refresh()` calls `upstream.getBudget()`, converts via `snapshotFromBudget(response)` (T-2.8's exported writer helper — one place defines the wire→snapshot mapping), writes to `kv_store` under `budgetKey(tenantId)` (`trust:budget:v1:<tenantId>`) with the configured TTL, and returns the fresh snapshot. Errors propagate to the caller so T-2.12's `POST /v1/admin/budget/refresh` operator route sees the failure verbatim. The internal `safeRefresh(trigger)` wraps the same call in try/catch for the boot and cron paths so a mothership outage never crashes the process — the failure logs as `error` with `{trigger, tenantId, error}` and the cache stays stale until the next tick. (3) **low-balance transition event** — `emitLowBalanceIfTransition()` fires a structured `warn` (`{tenantId, plan, tokensRemaining, tokensTotal, tokensResetAt}`) only on `false → true` (or first-ever refresh where `lowBalance === true`); repeat `true → true` ticks are silent so operators aren't paged nightly while a tenant hovers under the mothership's 10% threshold. `false → true → false → true` re-arms and fires again. (4) **read-through** — `getCached()` mirrors T-2.8's `TrustClient.getCachedBudget()` by calling the shared `parseBudgetSnapshot(raw, logger?)` (refactored to a module-level export on `trust-client.ts` during this task so the reader/writer parser can't drift); returns null on missing key, malformed payload, KV throw, or air-gap. T-2.12's `GET /v1/admin/budget` will call this directly. Air-gap semantics: `tenantId: null` → `start()` logs and no-ops, `refresh()` returns null without an upstream call, `getCached()` returns null — route wiring stays uniform across modes. Config surface added in `config.ts`: `BUDGET_REFRESH_CRON` (default `0 3 * * *`, host-local time; validated at boot via `cron.validate()` so a typo trips `ConfigError` instead of silently missing the schedule) and `BUDGET_TTL_SECONDS` (default 93 600 — 26 h, deliberately longer than the 24 h cadence so a missed refresh doesn't silently evict the row). Wired into `AppServices.budgetMeter` in `services.ts` (constructed with `tenantId: config.upstream?.tenantId ?? null`); `main()` calls `.start()` after `createApp()` so a failing warm refresh logs after route wiring; `closeAppServices()` calls `.stop()`. `apps/local/src/budget/index.ts` re-exports `BudgetMeter`, `NodeCronScheduler`, `BudgetMeterOptions`, `ScheduledJob`, `Scheduler`. Test coverage: 18/18 green — 3 refresh (happy-path with `vi.useFakeTimers` to freeze `cachedAt`, wire→snapshot mapping, KV TTL propagation; air-gap short-circuit; `UpstreamError` propagation to caller); 6 getCached (air-gap null, missing key, refresh→getCached round trip, malformed JSON warn, shape mismatch warn, KV get throw warn); 6 start/stop lifecycle (air-gap no-op, boot refresh + cron scheduling with expr assertion, `start()` idempotency, boot-refresh failure swallowed as `error` log while cron stays scheduled, cron-tick failure swallowed with `trigger: 'cron'` metadata, `stop()` idempotency + safe-pre-start); 3 low-balance transitions (first refresh with `lowBalance=true` fires warn once; steady `false` never fires; `false→true→true→false→true` fires exactly twice — verifies the transition guard). Total apps/local suite: **169/169 green**. Typecheck + build clean. **Coordination note for T-2.12:** `services.budgetMeter.getCached()` is the sole read path for `GET /v1/admin/budget`; `services.budgetMeter.refresh()` is the sole doorway for `POST /v1/admin/budget/refresh` — the admin route surfaces `UpstreamError` codes verbatim (503 for `upstream_not_configured`, 429 for `rate_limited`, etc.).
- ☑ **T-2.10** Landed `apps/local/src/migration/publish-to-mothership.ts` — the promotion door behind `POST /v1/migrate/publish?skill_id=<uuid>`. `PublishToMothershipClient({ upstream, pool, logger? }).publish(skillId)` runs the four-step pipeline in one call site: (1) **local lookup** — `SELECT id, name, slug, version, source, description, agent_summary, tags, category, schema_json, install_method, execution_layer, skill_md, source_url, repository_url, mcp_url, publisher_key_id, publisher_signature FROM skills WHERE id = $1`; a missing row throws a locally-minted `UpstreamError('not_found', ...)` so the route maps to 404 without a wasted mothership round trip. (2) **manifest build** via the exported pure helper `buildPublishRequest(row)` — maps required fields (`name`, `slug`, `version`, `source`, `execution_layer`) into `PublishRequest.manifest`; each `null` / empty-string is collected into a `missing_fields` list and thrown as a single `UpstreamError('bad_request', ..., { detail: { missing_fields } })` so the operator sees every gap in one shot (not "fix one, retry, fix next"). Optional fields (`description`, `agent_summary`, `tags`, `category`, `schema_json`, `install_method`, `skill_md`, `source_url`, `repository_url`, `mcp_url`) are folded in only when present so an explicit `null` never trips the mothership's Zod schema. D2 publisher-signature columns (`publisher_key_id`, `publisher_signature`) are threaded into `request.publisher_key_id` / `request.signature` verbatim so signed skills stay signed across the promotion boundary. (3) **upstream publish** — `upstream.publish(request)`; any `UpstreamError` propagates unchanged (`upstream_not_configured` in air-gap, `budget_exhausted`, `unauthenticated`, `rate_limited`, `upstream_unavailable`, etc.). (4) **persist** — `UPDATE skills SET mothership_skill_id, mothership_publish_status, mothership_published_at, mothership_url WHERE id = $5` — best-effort, so a 0-row match logs `warn` (mothership already accepted the manifest; caller still gets the response) and an UPDATE throw logs `error` and returns the response. Re-publish becomes a status-refresh, not a duplicate insert. Schema surface: migration `0033_mothership_publish.sql` adds four nullable columns (`mothership_skill_id TEXT`, `mothership_publish_status TEXT`, `mothership_published_at TIMESTAMP`, `mothership_url TEXT`) + partial index `idx_skills_mothership_skill_id ON skills (mothership_skill_id) WHERE mothership_skill_id IS NOT NULL` for fast "which local skills got promoted upstream" lookups; drizzle columns mirrored in `packages/schema/src/schema.ts` (`mothershipSkillId`, `mothershipPublishStatus`, `mothershipPublishedAt`, `mothershipUrl`); `SCHEMA_VERSION` bumped 32 → 33. Sync-source rows (glama, smithery, mcp-registry) stay NULL — only explicit promotions populate. Route surface: replaced the T-2.10 501 stub at `apps/local/src/routes/admin.ts` `POST /migrate/publish` with the wired handler — parses `?skill_id=`, empties → 400 `bad_request` before touching services, then delegates to `services.migrationClient.publish(skillId)`. `UpstreamError` codes surface via a lookup table `UPSTREAM_ERROR_STATUS: Record<UpstreamErrorCode, number>` at the top of the module (`upstream_not_configured` → 503, `budget_exhausted` → 402, `unauthenticated`/`forbidden` → 502 upstream-auth-problem-not-caller-side, `not_found` → 404, `rate_limited` → 429, `bad_request` → 400, `upstream_unavailable` → 503, `upstream_timeout` → 504); response body is `{ error: { code, message, retry_after?, detail?, request_id? } }` — same envelope the mothership emits so downstream tooling doesn't case-split on transport hop. Wired into `AppServices.migrationClient` in `services.ts` (constructed with `{ upstream, pool }`; air-gap posture inherited from `UpstreamClient` — no separate branch). Removed the stale `void services;` in `createAdminRoutes` since `services` is now used. Test coverage: 14/14 new green in `publish-to-mothership.test.ts` — 5 `buildPublishRequest` unit tests (required-field mapping, optional folding, D2 signature threading, missing-field aggregation into `detail.missing_fields`, empty-string treated as missing) + 9 client tests (empty skill_id short-circuits before pool, missing local row → `not_found`, incomplete manifest → `bad_request` without upstream call or UPDATE, happy path with SELECT+UPDATE positional-arg assertions, `UpstreamError('upstream_not_configured')` propagates without UPDATE, `UpstreamError('budget_exhausted')` preserves `retryAfter`, 0-row UPDATE returns response + warns, UPDATE throw returns response + errors, D2 signature threads into the upstream request). Routing test rewired at `routing.test.ts` (T-2.10 stub-marker assertion replaced): "serves POST /v1/migrate/publish through the wired T-2.10 handler" injects a `services.migrationClient.publish` stub that throws `UpstreamError('upstream_not_configured')` → asserts 503 + `error.code: 'upstream_not_configured'`, confirming the handler is wired end-to-end and the error taxonomy reaches the response body; added "rejects POST /v1/migrate/publish without a skill_id" → 400; the 401-without-token guard test kept. Total apps/local suite: **184/184 green** (was 169; +14 migration + +1 routing – 0 lost). Typecheck + build clean; schema build copies 33 SQL files. **Coordination note:** `POST /v1/publish` is the mothership contract path (from `@skillsregistry/contracts/upstream.ts` `PublishRequestSchema`); T-1.7 escalates any mothership-side shape drift.
- ☑ **T-2.11** Public routes (`src/routes/public.ts`) — sliced into three commits since the surface is a mix of thin proxies (T-2.11a) and fatter local-first handlers (T-2.11b / T-2.11c):
  - ☑ **T-2.11a** Landed the two thin mothership-proxy handlers. `POST /v1/trust/score` validates the body against `TrustScoreRequestSchema` (400 `bad_request` with `detail.issues` on schema failure; also 400 on non-JSON body), delegates to `services.trustClient.score(request)`, and returns only the `TrustScoreResponse` — the `budget` snapshot is admin territory (T-2.12). `GET /v1/leaderboards/:kind` parses the path param + a whitelist of well-known filters (`limit` — must be a positive integer, else 400; `category`, `ecosystem`, `skill_type` — empty strings dropped), delegates to `services.upstream.getLeaderboard(kind, params)`, and passes the mothership JSON body back verbatim (no re-validation — the mothership is the source of truth for leaderboard shape). Extracted the `UpstreamErrorCode → HTTP status` table + response-envelope helper from T-2.10's admin.ts into a new module `apps/local/src/http/upstream-response.ts` (`UPSTREAM_ERROR_STATUS`, `UpstreamErrorResponseBody`, `upstreamErrorBody(err)`, `upstreamErrorToResponse(err)`) so admin.ts + public.ts + future T-2.12 admin handlers share one taxonomy. `admin.ts` refactored to consume the shared helper (inline duplicate removed). The 501 stubs for `/v1/search`, `/v1/skills/:id`, `POST /v1/skills` now point at their sub-tasks (`task: 'T-2.11c'`, `task: 'T-2.11b'`, `task: 'T-2.11b'`). Test coverage: **+16 in new `public.test.ts`** — 6 trust/score (happy-path body verbatim, budget snapshot never leaks into public response, `budget_exhausted → 402 with retry_after`, `rate_limited → 429`, `upstream_timeout → 504`, optional manifest field forwarding) + 10 leaderboards (happy-path body verbatim, filter forwarding, empty-string filters dropped, non-integer / zero / negative / float limit rejected as 400 without touching upstream, `not_found → 404`, `upstream_unavailable → 503`, `unauthenticated → 502` upstream-auth-not-caller-side, `bad_request` detail forwarded). `routing.test.ts` rewired — the two T-2.11 stub-marker assertions replaced with wired-handler air-gap checks (both return 503 `upstream_not_configured`) + 3 bad_request smoke tests. Total apps/local suite: **203/203 green** (was 184; +16 public + +3 net routing – 0 lost). Typecheck + build clean.
  - ☑ **T-2.11b** Landed the two fatter local-first handlers on top of a new `SkillsClient` (`apps/local/src/skills/skills-client.ts`) that folds the pattern into one class the routes call into. `getSkill(id)` runs a positional-parameter SELECT against `skills` matching `id::text = $1 OR slug = $1 OR mothership_skill_id = $1` (three ways to name a skill); on hit, projects the 65-column row through `formatSkillDetail(row)` — snake_case → camelCase, null coalesce on nullable scalars, `shareUrl` falls back to `/skills/<slug>` when `mothership_url` is absent, bigint counters coerced from Postgres string, structured JSON columns re-serialized. On miss, delegates to `upstream.getSkill(id)`; **air-gap collapse** — when the upstream call throws `upstream_not_configured` the client re-mints `not_found` so callers see a truthful 404 (the local install *is* the world in air-gap mode) instead of a misleading 503. On upstream success, best-effort write-through: extracts `{ slug, name, version, source, executionLayer, id → mothership_skill_id, shareUrl → mothership_url, trustScoreV2, trustTier }` and issues `INSERT ... ON CONFLICT (slug) DO UPDATE SET mothership_skill_id, mothership_url, trust_score_v2, trust_tier, updated_at = NOW()`. Missing required cache columns, missing slug, non-object body, or DB failure — logs a warning and returns the upstream body verbatim (persistence is never on the critical path). `publishLocal(request)` INSERTs the full 18-column manifest projection with `status = 'published'` and `RETURNING id, slug, version, status`; pg error taxonomy — `23505` → `bad_request` with `detail: { slug, constraint: 'unique_violation' }`; other `23xxx` constraint violations → `bad_request` with `detail: { pg_code }`; unknown errors propagate. `SkillDetail` is aliased to `Record<string, unknown>` locally — the shape is enforced by construction in `formatSkillDetail`, and pulling `z.infer<typeof SkillDetailSchema>` would drag zod in as a direct dep for a type-only import. Wired into `AppServices.skillsClient` in `services.ts` (step 3l — constructed with `{ upstream, pool }`). `routes/public.ts`: the two 501 stubs for `GET /v1/skills/:id` and `POST /v1/skills` replaced with wired handlers that share the T-2.11a error mapper (`upstreamErrorToResponse`); POST validates against `PublishRequestSchema.safeParse` first (400 `bad_request` with `detail.issues` on schema failure; also 400 on non-JSON body). Test coverage: **+19 in new `skills-client.test.ts`** (10 getSkill: empty-id bad_request without touching pool/upstream, local hit, upstream fallback + write-through, air-gap collapse, non-air-gap error propagation, write-through DB failure non-fatal, write-through skipped on missing cache columns / missing slug / non-object body; 5 publishLocal: happy-path row projection, optional fields forwarded, `23505` → unique_violation detail, `23502` → pg_code detail, unknown errors propagate; 5 formatSkillDetail: snake→camel mapping, shareUrl fallback, null coalesce defaults, bigint coercion, structured JSON serialization) **+12 in `public.test.ts`** (7 GET /skills/:id: local-hit verbatim body, upstream-cached verbatim body, air-gap `not_found → 404`, `upstream_unavailable → 503`, `upstream_timeout → 504`, `rate_limited → 429 with retry_after`, `bad_request → 400`; 5 POST /skills: 201 happy path + LocalPublishResult body, invalid-body → 400 with issues, non-JSON → 400, unique-slug `bad_request → 400` with detail preserved, unrelated `upstream_unavailable → 503`) **+2 net in `routing.test.ts`** (removed 2 stub tests, added 4 wired-handler smoke tests: air-gap GET → 404, POST 201 success, POST bad_request on invalid body, POST bad_request on non-JSON). Total apps/local suite: **236/236 green** (was 203; +19 skills-client + +12 public + +2 net routing). Typecheck + build clean.
  - ☑ **T-2.11c** Landed `GET /v1/search` as a local-only pgvector + confidence-gate handler. **No upstream fallback** — the mothership has no `/v1/search` contract in `@skillsregistry/contracts/upstream.ts`, so the "optional upstream fallback" phrase in the original scope collapsed to N/A on recon; the handler is truthful local-only, and the deep-search / reranker knobs (T3 LLM rescue + cross-encoder rerank) default to `false` with **throw-on-call stub backends** so a mis-configured `SEARCH_DEEP_ENABLED=true` / `SEARCH_RERANKER_ENABLED=true` fails loud rather than degrading silently. New module `apps/local/src/search/` with six files: `stub-llm.ts` (throw-on-call `LlmAdapter`, identity `stub-llm:disabled`), `stub-reranker-backend.ts` (throw-on-call `RerankerBackend`, identity `stub-reranker:disabled`), `pg-search-cache.ts` (implements `SearchCachePort` over `PgKv` — key format `search:v1:<tenantId>:<appetite>:<sha256(lowercase(trim(query)))>`; tier-biased TTL 3600/1800/600s for T1/T2/T3; strips `enrichmentPromise` before JSON.stringify; swallows all errors → miss/silent), `noop-search-logger.ts` (implements `SearchLoggerPort` — `buildLogEntry` builds a correct `SearchLogEntry` from `SearchLoggerInput`, `log()` is a no-op, `estimateEmbeddingCost` returns 0 since Ollama is host-local), `search-service.ts` (`SearchService` class + pure `projectResponse` mapper — domain `FindSkillResponse` → contract `SearchResponse`: `results[].agentSummary → skills[].description`, `meta.cacheHit → cached`, `meta.llmInvoked → deepSearchUsed`, `source: 'local'` on both skill + meta, `signals: []` since `FindSkillResponse.meta` doesn't carry per-source scores, `confidence = clamp01(topScore || 0)`, `category: null` since `SkillResult` doesn't carry it), plus a barrel `index.ts`. `config.ts`: added `SearchConfig` block with 11 knobs (fusion mode, T1/T2 thresholds, deepSearch + reranker toggles, default appetite, circuit-breaker threshold + cooldown, cache TTL per tier) — env-var prefixes `SEARCH_*`; added `parseFloatMin`, `parseAppetite`, `parseSearch` helpers; wired into `loadConfig`. `services.ts`: added step 3m — constructs `PgVectorProvider` + `PgSearchCache` + `NoopSearchLogger` + `embedFn` (bridges `Float32Array → number[]` via `Array.from(...)` since `EmbedderAdapter` returns typed arrays but `ConfidenceGate` expects `number[]`) + `StubLlmAdapter` + `StubRerankerBackend` + three separate `CircuitBreaker`s (one each for DeepSearch, Reranker, CompositionDetector so a failing subsystem doesn't blast a shared breaker) + `DeepSearch` + `Reranker` + `CompositionDetector` + `ConfidenceGate` + `SearchService`; the new `searchService: SearchService` field is exposed on `AppServices`. `routes/public.ts`: the T-2.11a 501 stub for `GET /v1/search` replaced with the wired handler — validates `?q=` (required, non-empty after trim → 400 `bad_request`), `?limit=` (integer 1..50 → 400 otherwise), `?appetite=` (whitelist `strict|cautious|balanced|adventurous` → 400 otherwise), `?tags=` (comma-separated string[], trimmed, empties dropped), `?category=` (scalar), `?runtime_env=` (comma-separated string[]), `?visibility=` (whitelist `public|private|unlisted`), `?portable=` (`true|false|1|0` → boolean, 400 on anything else); delegates to `services.searchService.search(query, opts)` with `tenantId` sourced from `getTenantId(c) ?? 'local'`. Test coverage: **+11 in new `search/pg-search-cache.test.ts`** (3 `cacheKey`: trim+lowercase equivalence, differs by tenant + appetite, v1 prefix; 8 `PgSearchCache`: tier-1/2/3 TTL biasing, strips `enrichmentPromise`, parses JSON on get, null on `kv.get` error, null on invalid JSON, swallows `kv.put` errors) **+10 in new `search/search-service.test.ts`** (8 `projectResponse`: description mapping, `source=local` on both skill + meta, `cacheHit → cached` + `llmInvoked → deepSearchUsed`, confidence clamp to 1, confidence = 0 on empty results, `signals: []`, `category: null`, publisher-signing fields default to null; 2 `SearchService.search`: delegates with mapped options + `tenantId`, propagates gate errors) **+16 in `public.test.ts`** (1 delegation happy-path, 1 `X-Tenant-Id` threading, 7 query-param parsing — limit, appetite, tags trim+split, runtime_env + category, visibility, portable true/0; 7 validation errors — missing q, whitespace-only q, non-integer limit, limit out of 1..50 range, unknown appetite, unknown visibility, unknown portable; 1 unexpected-error → 500 propagation) **+2 net in `routing.test.ts`** (added `budget` + `search` blocks to `buildConfig`; replaced the 501-stub assertion with two wired-handler smokes — 200 through mocked `searchService.search`, and missing-q → 400). Total apps/local suite: **274/274 green** (was 236; +11 pg-search-cache + +10 search-service + +16 public + +2 net routing = +39, minus 1 stub test that was removed with the wired handler). Typecheck + build clean.
  - ☑ `GET /v1/health` — liveness (checks DB reachability + reports embedder + upstream configured) — landed with T-2.3
- ☑ **T-2.12** Admin routes (`src/routes/admin.ts`) — protected by `ADMIN_TOKEN` bearer:
  - ☑ `GET /v1/admin/budget` — returns `{ budget: BudgetSnapshot | null, mode: 'configured' | 'air_gapped' }`. Reads `services.budgetMeter.getCached()` which never throws (returns null on missing key / malformed payload / KV throw / air-gap). `mode` disambiguates a cold cache from air-gap so operators can tell them apart without probing config. Always 200.
  - ☑ `POST /v1/admin/budget/refresh` — short-circuits to 503 `upstream_not_configured` in air-gap without touching the meter. On configured mothership, delegates to `services.budgetMeter.refresh()` (throws through — unlike the cron callback which swallows). `UpstreamError` routed through shared `upstreamErrorToResponse` mapper; unexpected `Error` → 500 `internal_error`.
  - ☑ `GET /v1/admin/health` — deep health across four probes in parallel: (1) DB via `pool.query('SELECT 1')`; (2) embedder via `services.embedder.embed('.')` reporting the `.identity.id`; (3) mothership passively via `services.upstream.isAirGapped` + `.circuitState` — no active `getBudget()` call, to avoid burning mothership budget on every probe; (4) migrations via `SELECT COALESCE(MAX(version), 0) FROM schema_migrations` vs `SCHEMA_VERSION` from `@skillsregistry/schema`. Aggregate is `ok` only when db + embedder + migrations are all `ok`; mothership does NOT gate aggregate (air-gap is a valid healthy posture, circuit-open is a mothership-side signal). 200 on `ok`, 503 on `degraded` — usable directly as a container probe.
  - ☑ `POST /v1/migrate/publish` — the migration door (landed with T-2.10).
  - Threaded `pool` as a third arg to `createAdminRoutes(services, adminToken, pool)` — mirrors `createApp(config, pool, services)`; keeps the pg pool outside `AppServices`. `index.ts` and `routing.test.ts` updated. Tests: 15 new in `admin.test.ts` (budget cached/cold/air-gap, refresh air-gap/success/UpstreamError/unexpected, health ok/db-down/embedder-down/schema-behind/circuit-open/air-gap, auth guard); 3 existing routing.test.ts 501-stub tests rewritten to exercise the wired handlers. Total 289 tests, all green. Typecheck + build clean.
- ☑ **T-2.13** Wired `POST /mcp` on the Hono sub-app to `handleMcpRequest` from `@skillsregistry/mcp`. Composition root gained `mcpAdapters: McpAdapters` + `mcpConfig: ResolvedMcpConfig` on `AppServices`, built inside `buildAppServices` alongside the rest of the domain graph (block `3n`). Adapter bundle sits in `apps/local/src/mcp/`: `McpSearchGateway` wraps `ConfidenceGate` with `NodeAfterResponse` baked in so the framework-agnostic `SearchGatewayPort` still gets deferred-write coverage; `McpSkillLookup` delegates to the existing `SkillsClient` (local-first + upstream fallback + air-gap collapse already live there) and maps `UpstreamError('not_found')` → `{ found: false }`; `McpLeaderboardProxy` calls `UpstreamClient.getLeaderboard(kind, params)`, unwraps the `{ leaderboard: [...] }` envelope, and swallows `upstream_not_configured` → `[]` so MCP clients see "no rankings available" instead of a JSON-RPC error leaking the air-gap posture; `McpCompositionLookup` returns `{ found: false }` always (no local composition index in MVP → tool surfaces the miss via `isError: true` per MCP 2025-03-26 §tools/call). `buildMcpAdapters()` also constructs the SqlPool-backed invocation recorder (`createSqlPoolInvocationRecorder({ pool, argsMaxChars })`) and threads `NodeAfterResponse` into the bundle so the dispatcher writes `mcp_invocations` + bumps `agent_invocation_count` outside the request critical path. `McpConfig` block added to `config.ts` with 12 env vars (`MCP_SERVER_NAME`, `MCP_SERVER_VERSION`, `MCP_CANONICAL_ORIGIN`, `MCP_DOCUMENTATION_URL`, `MCP_OPENAPI_URL`, `MCP_SEARCH_DEFAULT_LIMIT` = 10, `MCP_SEARCH_MAX_LIMIT` = 50, `MCP_SEARCH_QUERY_MAX` = 500, `MCP_LEADERBOARD_DEFAULT_LIMIT` = 20, `MCP_LEADERBOARD_MAX_LIMIT` = 100, `MCP_BATCH_MAX` = 20, `MCP_INVOCATION_ARGS_MAX` = 4096 chars). The Hono handler is thin — parses JSON body (throw → `parseErrorResponse()` -32700 in a 400), builds `DispatchContext { tenantId: getTenantId(c) ?? 'local', adapters, config }`, calls `handleMcpRequest`, then switches on `DispatchOutcome`: `json` → 200 JSON, `accepted` → 202 empty body (notifications + empty-response batches per MCP 2025-03-26 §Transports), `error` → JSON at declared status. `X-Tenant-Id` is threaded into the dispatch context via the existing `tenantContext` middleware (advisory scope hint, not a security boundary in v1). Test coverage: 13 new in `apps/local/src/routes/mcp.test.ts` covering `tools/list` fan-out, per-tool happy path (`search_skills`, `get_skill`, `get_trust_breakdown`, `list_leaderboard`, `resolve_composition`), tool-domain misses (`isError: true` inside success envelope, no `resolvedSkillId` stamped, recorder logs `succeeded: false`), skill-resolving-tool recorder wiring (`get_skill`/`get_trust_breakdown` stamp `resolvedSkillId`; discovery tools stamp `null`), `X-Tenant-Id` propagation, unknown method → `-32601`, unknown tool name → `-32601`, batch dispatch preserving ids, empty batch → 400 with `-32600`. Wire smoke in `routing.test.ts` also updated (initialize returns `2025-03-26` + `serverName: skillsregistry-local`, parse-error `-32700`, notification → 202). Total apps/local suite: **304/304 green**. Typecheck clean.
- ☑ **T-2.14** Discovery descriptor wired at `apps/local/src/routes/mcp.ts` via `buildDiscoveryDescriptor` from `@skillsregistry/mcp`. `GET /mcp.json` and `GET /.well-known/mcp.json` (RFC 8615 alias) share one handler function so future descriptor tweaks land in one place. The handler passes `{ requestUrl: c.req.url, config: services.mcpConfig }`; the descriptor builder prefers `config.canonicalOrigin` when set (production deploy) and falls back to the request origin (local dev) — same semantics the mothership uses at `api.skillsregistry.net/.well-known/mcp.json`. Shape emitted: `schemaVersion: '1'`, `protocolVersion: '2025-03-26'`, `serverInfo { name, version }` from `MCP_SERVER_NAME` / `MCP_SERVER_VERSION`, `transport { type: 'streamable-http', methods: ['POST'], endpoint: '${origin}/mcp' }`, `capabilities { tools: { listChanged: false } }`, `auth { model: 'none', tenantHeader: 'X-Tenant-Id' }` (v1 read-only posture — advisory scope, not a security boundary), `tools[]` mirroring the same 5 tool definitions the dispatcher exposes (`search_skills`, `get_skill`, `get_trust_breakdown`, `list_leaderboard`, `resolve_composition`) each with inputSchema, `documentation` + `openapi` URLs from `MCP_DOCUMENTATION_URL` / `MCP_OPENAPI_URL` when configured. Tests: 7 new in `apps/local/src/routes/mcp.test.ts` (schemaVersion + protocolVersion + serverInfo emission, streamable-http transport with request-origin fallback, `canonicalOrigin` override wins over request URL, `documentationUrl` + `openapiUrl` overrides land in the descriptor, auth posture `{ model: 'none', tenantHeader: 'X-Tenant-Id' }`, all 5 tools advertised with schemas, `.well-known/mcp.json` returns byte-identical descriptor); 2 stub tests in `routing.test.ts` replaced with real descriptor smoke asserting `protocolVersion: '2025-03-26'`, `transport.type: 'streamable-http'`, endpoint ends with `/mcp`, 5 tool names present. `AdapterOverrides` in `mcp.test.ts` extended with `mcpConfig?: Partial<ResolvedMcpConfig>` for per-test config injection. Total apps/local suite: **311/311 green**. Typecheck clean.
- ☑ **T-2.15** Structured logging landed with `pino@9` (json default, `pino-pretty` opt-in via `LOG_FORMAT=pretty`) plus a request-id middleware at `apps/local/src/middleware/request-logger.ts`. `LogConfig` gained `format: 'json' | 'pretty'` and `requestIdHeader: string` (RFC 7230 tchar guard, ≤64 chars) driven by two new env vars `LOG_FORMAT` and `LOG_REQUEST_ID_HEADER` (default `X-Request-Id`). New `apps/local/src/logging/` module: `createLogger(config)` returns the root `PinoLogger` (level from `LOG_LEVEL`; pretty transport wired when configured), `createSilentLogger()` for tests, and `adaptToPortLogger(pinoChild)` bridges pino's `(mergingObject, message)` arg order into the `PortLogger { info(msg, meta), warn, error }` shape the five existing domain modules (`trust-client`, `budget-meter`, `publish-to-mothership`, `skills-client`, `boot-schema`) already accept — no interface changes needed downstream, just one bridge in `buildAppServices`. `AppServices` gained a `logger: PinoLogger` field; `buildAppServices(config, pool, logger)` picks up the root logger, and each module gets a child scoped to `{ module: '<name>' }` fed through `adaptToPortLogger` so log lines carry module context. `createApp` mounts `requestLogger` first so the summary line covers the full handler duration (including `tenantContext` + admin bearer auth); the middleware honors an inbound `X-Request-Id` (validated against `/^[A-Za-z0-9._-]{1,128}$/`) or mints `crypto.randomUUID()`, stashes `requestId` + a `PinoLogger` child (bound to `{req_id, method, path}`) on `c`, echoes the id on the response, and emits one line after `await next()` with `status`, `duration_ms` (from `process.hrtime.bigint()`), `tenant_id` (from `getTenantId(c)` — safe on routes without `tenantContext`), and an `err` field pulled from `c.error` when Hono caught a throw. Level selection is status-based: 5xx / thrown → ERROR, 4xx → WARN, else INFO (Hono catches handler errors internally before middleware `try/catch` can see them, so the classic try/catch pattern doesn't work here — the access-log posture does). `main()` boots pino before the logger-less phases (`ConfigError` still uses `console.error` since it's upstream of `createLogger(config.log)`); after that `bootLogger.error()` covers schema-boot + services-init failures and `bootLogger.info()` replaces both `console.log` calls (listen + shutdown). Middleware falls back to a silent logger when `services.logger` is missing so unit tests passing `{} as AppServices` don't crash inside `logger.child(...)` — real boot always sets it. Tests: 7 new in `apps/local/src/logging/logging.test.ts` (createLogger honors level, silent factory works, adaptToPortLogger flips arg order + preserves child bindings), 8 in `apps/local/src/middleware/request-logger.test.ts` (UUID mint when header absent, honors valid inbound id, rejects overlong/illegal, custom header via `LOG_REQUEST_ID_HEADER`, tenant_id propagation, ERROR line + 500 on handler throw, child-logger stash on context, numeric `duration_ms`), plus 6 new `config.test.ts` cases covering `LOG_FORMAT` accept/reject and `LOG_REQUEST_ID_HEADER` accept/reject-invalid-chars/reject-overlong. Three existing route test factories (`routing.test.ts`, `mcp.test.ts`, `public.test.ts`) updated to supply the new `LogConfig` fields; the `NULL_SERVICES` casts benefit from the middleware's silent-logger fallback so they didn't need per-fixture logger stubs. Total apps/local suite: **331/331 green**. Typecheck + build clean.
- ☑ **T-2.16** Docker Compose polished. The scaffolded `apps/local/docker-compose.yml` from T-2.1 declared the three services but was light on env passthrough (no LOG_/MCP_ vars, no `.env` doc) and skipped the design §11 step 3 "warm the Ollama model" phase — first `docker compose up` would let the app race an empty Ollama, blowing up cold `/v1/search`. This task closes both. Four services now: `postgres` (`pgvector/pgvector:pg16`, named volume `postgres-data`, `pg_isready` healthcheck), `ollama` (`ollama/ollama:latest`, named volume `ollama-models` for the model cache, `ollama list` healthcheck), `ollama-init` (one-shot `ollama/ollama:latest` — waits ≤60s for the server, runs `ollama pull ${OLLAMA_EMBEDDING_MODEL:-nomic-embed-text}` against `OLLAMA_HOST=http://ollama:11434`, exits; idempotent thanks to the shared volume; `restart: "no"`), and `app` (built from `apps/local/Dockerfile`, `depends_on` all three with `postgres: service_healthy` + `ollama: service_started` + `ollama-init: service_completed_successfully` so it never boots against a cold model). Env passthrough now covers Core (`NODE_ENV`, `HOST`, `PORT`, `DATABASE_URL`, `ADMIN_TOKEN` — required, uses `${ADMIN_TOKEN:?ADMIN_TOKEN is required — set it in .env}` fail-fast), Postgres pool (`POSTGRES_POOL_MAX`, `POSTGRES_STATEMENT_TIMEOUT_MS`), Embedder (`EMBEDDER`, `OLLAMA_URL`, `OLLAMA_EMBEDDING_MODEL`), Upstream (`MOTHERSHIP_URL`, `MOTHERSHIP_API_KEY`, `TENANT_ID`, `UPSTREAM_SEARCH_FALLBACK`), Logging (`LOG_LEVEL`, `LOG_FORMAT`, `LOG_REQUEST_ID_HEADER`), and MCP (`MCP_CANONICAL_ORIGIN`, `MCP_DOCUMENTATION_URL`, `MCP_OPENAPI_URL`). Search-tuning + MCP-limit vars still fall back to their `src/config.ts` defaults and can be added to `.env` as needed — enumerated in `.env.example` (commented) rather than the compose file to keep the compose surface readable. Compose reads `apps/local/.env` automatically (git-ignored; tracked template at `.env.example`); every `${VAR:-default}` in the file resolves from that env. `.env.example` rewritten to advertise the full var surface (Required / HTTP / Postgres pool / Artifact / Budget poller / Embedder / Upstream / Logging / MCP / Search tuning) with defaults inlined so operators see the tuning knobs without cracking open `src/config.ts`. Validated locally with `docker compose config` — clean parse; unset `ADMIN_TOKEN` correctly fails at interpolation time. Docker build/run not exercised in this task (that's T-2.17 Dockerfile + T-2.18 air-gap smoke).
- ☑ **T-2.17** Dockerfile polished. Multi-stage layout already scaffolded at T-2.1 (build → runtime, non-root `app:app`, wget healthcheck on `/v1/health`) — this task closed the remaining gaps and delivered a functioning image end-to-end. Fixed a latent build break: the build stage was missing `COPY scripts ./scripts`, so `packages/schema`'s postbuild step (`node ../../scripts/copy-sql.mjs src/migrations dist/migrations`) exploded with `MODULE_NOT_FOUND` on the first real `docker build`. Added the copy so migration SQL is bundled into the pruned `/out` tree. New `apps/local/entrypoint.sh` (chmod +x, `#!/bin/sh`, `set -eu`) documents the design §11 five-phase startup sequence via `echo` lines before `exec node --enable-source-maps ./dist/index.js` — the sequence itself is executed inline inside the Node process (Postgres reachability gated by compose `service_healthy`; migrations applied by `bootSchema` before HTTP bind, fail-fast on error; Ollama model warmed by the `ollama-init` sidecar; mothership connectivity probed non-blocking by the budget-meter warm-refresh; server binds last). `exec` replaces the shell so Node runs as PID 1 and receives SIGINT/SIGTERM directly for the existing shutdown handler in `src/index.ts`. `--enable-source-maps` keeps prod stack traces pointing at `.ts` line numbers — free win for on-call debugging. Dockerfile switched from `CMD ["node", ...]` to `ENTRYPOINT ["/app/entrypoint.sh"]` with `COPY --chown=app:app --chmod=0755 apps/local/entrypoint.sh /app/entrypoint.sh`. Added five OCI image labels (`org.opencontainers.image.title`, `.description`, `.source`, `.licenses=Apache-2.0`, `.vendor=Cognium Labs`) for supply-chain identification. Verified end-to-end: `docker build -f apps/local/Dockerfile .` succeeds; `docker inspect` confirms `User: app` (uid=100, gid=101), `Entrypoint: [/app/entrypoint.sh]`, healthcheck on `/v1/health`, exposed `3000/tcp`, `NODE_ENV=production`; `docker run --rm skillsregistry-local:t217` prints the five-phase entrypoint banner then dies with `Invalid configuration (2 issues): DATABASE_URL: required, ADMIN_TOKEN: required` and `exit=1` — fail-fast posture intact; `wget` is present at `/usr/bin/wget` for the healthcheck; `/app/entrypoint.sh` lands executable owned by `app`.
- ☑ **T-2.18** Air-gap smoke test tooling shipped as `apps/local/scripts/smoke-airgap.sh` (bash + curl + jq, `chmod +x`, wired into `package.json` as `pnpm --filter @skillsregistry/local smoke:airgap`). Five hard assertions against a live node with `MOTHERSHIP_URL` unset: (1) `GET /v1/health` — polls up to `HEALTH_TIMEOUT` (default 60s) then asserts `status: "ok"`, `upstreamConfigured: false`, `dbReachable: true`; (2) `GET /v1/search?q=example&limit=5` — 200 with `.skills` array present; (3) `POST /mcp` with `{"jsonrpc":"2.0","id":1,"method":"tools/list"}` — 200 with exactly 5 tools advertised (`search_skills`, `get_skill`, `list_leaderboard`, `get_trust_breakdown`, `resolve_composition`); (4) `POST /v1/skills` with a minimal manifest wrapped in `{ manifest: ... }` (auto-generates a unique slug per run via `SMOKE_SLUG=airgap-smoke-$(date +%s)` so re-running against the same Postgres doesn't collide on `unique(slug)`) — response carries `.id` (mothership contract shape); (5) `POST /v1/trust/score` with `{ "skill_id": "<id>" }` — expects **HTTP 503** with `.error.code = "upstream_not_configured"` (fail-open would be a bug). Script uses `set -euo pipefail` + coloured `step`/`pass`/`fail` markers; on failure prints the offending response body and exits non-zero at the first bad step. Configurable via `BASE_URL` (default `http://localhost:3000`), `HEALTH_TIMEOUT`, `SMOKE_SLUG` env vars. First green live run landed with T-2.19 below: `docker compose down -v && docker compose up -d && ./scripts/smoke-airgap.sh` → all 5 assertions pass in ~40s from empty volume.
- ☑ **T-2.19** Schema bootstrap shipped as `packages/schema/src/migrations/0000_skills_base.sql`. The extracted migration set assumed `skills` already existed (0001's own comment: "managed by the broader Runics platform"), which held in the mothership repo where an earlier Drizzle bootstrap created it but not here — `bootSchema(pool)` failed on 0001 against an empty Postgres. Rejected the drizzle-kit path (option b in the original T-2.19 stub): the type-checked Drizzle definition at `schema.ts:33-203` reflects the *final* v6.2 shape (130+ columns), but many of those columns are added by later ALTER migrations with their own CHECK constraints and defaults; generating the full table at bootstrap time collides with 0005/0010/etc. constraint rewrites. Shipped option (a) instead, but scoped to the **pre-0004 baseline** shape — the smallest column set that lets 0001–0033 apply in order. Columns seeded: `id` (UUID PK), `name`, `slug` (UNIQUE), `version`, `source`, `description`, `agent_summary`, `alternate_queries`, `schema_json`, `auth_requirements`, `install_method`, `capabilities_required`, `trust_score`, `execution_layer`, `content_safety_passed`, `tags`, `category`, `cognium_scanned` (legacy BOOLEAN referenced by 0012's UPDATE — see CLAUDE.md's known-issue note), `created_at`, `updated_at`. Deliberately omitted from bootstrap: `status` / `type` (added by 0005 with their own CHECKs — front-loading collides), and the 90+ columns added by 0004–0033 (those live in their own migrations). Indexes seeded: `trust_score`, `source`, `execution_layer` (the leaderboard-hot columns referenced by 0009's materialized views). Extension seeded: `pgcrypto` for `gen_random_uuid()`. All statements are idempotent (`CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS` + `CREATE EXTENSION IF NOT EXISTS`) so existing mothership databases with the table already present no-op cleanly. Runner picks it up automatically: `loadBundledMigrations()` sorts filenames lexicographically, `0000_` comes first, applies before 0001. `SCHEMA_VERSION` stays 33 (bootstrap records itself as version 0). During verification the initial 14-column bootstrap surfaced two more gaps requiring iteration: 0005's `CREATE INDEX ... USING gin(tags)` needed `tags TEXT[]` seeded (the pre-0005 baseline included it — confirmed by 0005's own comment "tags already exists as text[], skip it"), and 0012's `UPDATE skills SET cognium_scanned = false WHERE cognium_scanned = true` needed the legacy boolean seeded even though production paths use `cognium_scanned_at IS NULL` (CLAUDE.md known issue). Final bootstrap = 20 columns / 3 indexes / 1 extension. Also fixed two field-name drift bugs in `smoke-airgap.sh` surfaced during the first live green run: `/v1/search` returns `.skills[]` (not `.results[]`), and `POST /v1/skills` returns `.id` (not `.skill_id`) though `POST /v1/trust/score` requests still take `{ "skill_id": "..." }` per `TrustScoreRequestSchema` in `@skillsregistry/contracts/upstream`. End-to-end verification: `docker compose down -v` → `docker compose up -d` → 34 migrations applied cleanly (`applied 0000_skills_base.sql` through `applied 0033_mothership_publish.sql`, `[schema] boot: complete`) → `./scripts/smoke-airgap.sh` all 5 assertions green.

## Phase 3 — Admin web UI (`apps/local/web/`)

- ☑ **T-3.1** Scaffolded Astro 5 at `apps/local/web/` with `output: 'static'`, `base: '/admin'`, `trailingSlash: 'always'`. Pivoted from `@astrojs/node` (SSR) to a **static bundle mounted by Hono** — kept the deploy model at one process / one port (per plan decision 2) rather than mounting a second Node adapter alongside Hono. `@tailwindcss/vite@4` for styling (v4 with the `@theme` syntax matches mothership). Workspace pkg `@skillsregistry/local-web` added to `pnpm-workspace.yaml`; `apps/local/package.json` gained `web:build` / `web:dev` scripts; `apps/local/Dockerfile` now `COPY apps/local/web` and preserves `apps/local/web/dist` in the runtime stage.
- ☑ **T-3.2** Loopback-only auth model shipped — no login page, no cookie session. Two-piece design: (a) new `src/middleware/loopback-only.ts` hard-rejects non-`127.0.0.1` / `::1` / `::ffff:127.0.0.1` origins on `/admin/*` with 403 `{error: {code:'forbidden', message:'admin UI is loopback-only'}}` (fails **closed** if `remoteAddress` is missing); (b) `src/middleware/admin-auth.ts` gained a loopback exception so `/v1/admin/*` and `/v1/migrate/*` skip Bearer verification for loopback traffic while over-network callers still need the token (unchanged CI/CD contract). `src/index.ts` mounts `loopbackOnly()` + `serveStatic({root:'./web/dist', rewriteRequestPath: p => p.replace(/^\/admin/, '') || '/index.html'})` from `@hono/node-server/serve-static` after the admin sub-app. Test coverage: 4 new in `loopback-only.test.ts` (three loopback forms pass, `192.168.1.42` gets 403) + 3 new in `admin-auth.test.ts` for the exception branch. `curl http://127.0.0.1:3000/admin/` returns 200 HTML; `curl http://<lan-ip>:3000/admin/` returns 403.
- ☑ **T-3.3** Dashboard at `apps/local/web/src/pages/index.astro` — three cards (health probes, budget gauge, recent activity placeholder) hydrated by a vanilla `<script>` block that fetches `/v1/admin/health` + `/v1/admin/budget` on `DOMContentLoaded` and populates skeleton divs. Reusable `BudgetGauge.astro` renders an emerald→amber→red CSS progress bar keyed off `remaining/total`; `StatusPill.astro` renders `ok`/`degraded`/`unknown` pills. Auto-refresh every 30s via `setInterval`. Verified against the running Hono at `:3000/admin/` — pills go green, gauge fills.
- ☑ **T-3.4** Skills list at `apps/local/web/src/pages/skills/index.astro` — read-only view over `GET /v1/admin/skills?limit=100`. Added the endpoint at `src/routes/admin.ts` (`SELECT id, slug, name, source, version, mothership_publish_status, mothership_url, mothership_published_at, created_at FROM skills ORDER BY created_at DESC NULLS LAST, id LIMIT $1 OFFSET $2` + parallel `SELECT COUNT(*)::text AS c FROM skills` for total; `clampInt()` gates `limit` to 1-500 and `offset` to non-negative, returning 400 `bad_request` on garbage). New `AdminSkillsListResponseSchema` in `packages/contracts/src/responses.ts` (patch bump changeset at `.changeset/contracts-admin-skills-list.md`; npm publish deferred until Phase 3 fully lands — local workspace consumers work via `workspace:*`). Shared `SkillsTable.astro` skeleton with a `mode: 'list' | 'migrate'` prop toggles the mothership columns. 10 new admin-route tests covering happy path, camelCase projection, mothership metadata preservation, clamp-to-max, 400 on non-integer / negative / zero, 500 on DB throw, Date-instance ISO coercion. All 384 apps/local tests + 87 contracts tests green.
- ☑ **T-3.5** Budget page at `apps/local/web/src/pages/budget.astro` — dedicated view with a full table of `total`/`remaining`/`lowBalance`/`tokensResetAt`/`updatedAt` from `GET /v1/admin/budget`. "Refresh from mothership" button POSTs `/v1/admin/budget/refresh`, shows loading state, re-renders on success. In air-gap mode the endpoint returns 503 `upstream_not_configured`; the page surfaces this in a warning banner ("mothership not configured — cannot refresh") without breaking the read side. Reuses `BudgetGauge.astro` from T-3.3.
- ☑ **T-3.6** Migration page at `apps/local/web/src/pages/migration/index.astro` — same skill list as T-3.4 but with the `SkillsTable mode="migrate"` shape and per-row action column. `actionCell()` renders a `<a rel="noopener noreferrer" target="_blank" href="{mothershipUrl}">` when published + URL exists; otherwise a "Publish" button that POSTs `/v1/migrate/publish?skill_id=<uuid>` and shows a success/failure banner. On success the page re-fetches so the status pill transitions to `published`. `PublishStatus` type in `dashboard-client.ts` normalises `null` / `'unpublished'` / `'pending'` / `'published'` / `'failed'` → `unknown` fallback for defensive rendering.
- ☑ **T-3.7** MCP wiring page at `apps/local/web/src/pages/mcp/index.astro` — four surfaces on one page: (a) **Endpoint card** with transport (`streamable-http (POST)`), endpoint URL, tenant header (`X-Tenant-Id` advisory), auth model (`none` in v1 read-only); (b) **Discovery status card** that fetches `/mcp.json` at load and renders a pill (`N tools · serverInfo · protocol 2025-03-26`) plus the tool name/description list; (c) **curl snippet** for a `tools/list` smoke test with `x-tenant-id: local` header; (d) **Claude Desktop config snippet** ready to paste into `~/.config/Claude/claude_desktop_config.json` — uses `npx mcp-remote <origin>/mcp` per the MCP client spec, restart Claude Desktop and the five tools surface. Both snippets go through a new `CodeBlock.astro` component with a delegated copy-to-clipboard button (`navigator.clipboard.writeText`, idle/done label swap for 1.5s, falls through silently if the clipboard API is blocked). The static bundle uses an `__ORIGIN__` sentinel that a client-side `rewriteSnippet()` swaps for `window.location.origin` on `DOMContentLoaded` — so the same `dist/` works at any origin without an Astro Node adapter. Verified against `pnpm --filter '@skillsregistry/local-web' build` — 6 pages built (index, health, budget, skills, migration, mcp), total client JS < 15KB gzipped.
- ☑ **T-3.8** Design tokens shipped at `apps/local/web/src/styles/global.css` — verbatim snapshot of mothership `web/src/styles/global.css` (emerald `#6ee7b7` on near-black `#0a0a0b`, Inter + JetBrains Mono via Fontsource-Variable, `--color-{bg,text,accent,border}-*` variable set). Sacred-boundary compliant: copy, not import — mothership repo stays read-only. Consumed via Tailwind v4's `@theme` block so every utility class (`text-primary`, `bg-elevated`, etc.) reads the token instead of a hardcoded hex. All six pages render on the emerald palette with mothership-parity typography.

## Phase 4 — Release + docs

- ☑ **T-4.1** `apps/local/README.md` rewritten around a quickstart-first structure. Old README was ~50 lines of "here are the commands" — new one is ~230 lines organised for the two audiences (operators standing up a private node + developers hacking on the codebase). Sections: (1) one-paragraph value prop + "what you get on `docker compose up`" bullet list surfacing all four public surfaces (skills API, MCP, admin UI, connected-mode passthrough); (2) **Quickstart (5 minutes)** — `git clone` → `cp .env.example .env` → `docker compose up -d` with `openssl rand -hex 32` recipe for `ADMIN_TOKEN` and expected first-boot pull sizes (`postgres` 150MB / `ollama` 1GB / `nomic-embed-text` 275MB); (3) **Verify** — five copy-pasteable `curl` commands hitting `/v1/health` → `POST /v1/skills` → `/v1/search` → `POST /mcp tools/list` → `open http://localhost:3000/admin/`, plus a one-liner for the packaged `smoke:airgap` script; (4) **Modes: air-gap vs connected** — exact env matrix for each posture, effect on every endpoint (`/v1/trust/score`, `/v1/leaderboards/*`, `/v1/migrate/publish`, `/v1/search` fallback), and the `docker compose up -d --force-recreate app` rev needed to re-read `.env`; (5) **Auth model** — loopback (127.0.0.1/::1) skips bearer for admin/migration; over-network requires `Bearer $ADMIN_TOKEN` and the UI stays 403 regardless (with the `-p 127.0.0.1:3000:3000` Docker binding tip for CI/automation deploys); (6) **Admin UI** — six-page table with routes; (7) **MCP client wiring** — bare Claude Desktop `~/.config/Claude/claude_desktop_config.json` JSON block using `npx mcp-remote` + all 5 tool descriptions with air-gap fallback notes; (8) **Local development** — `pnpm install` + `docker compose up -d postgres ollama ollama-init` (partial-compose pattern) + `pnpm --filter @skillsregistry/local dev` + admin UI dev server + `pnpm -w -r test` (1024 tests, ~1s); (9) **Environment variables** — required-vs-default table with `DATABASE_URL`, `ADMIN_TOKEN`, `PORT`, `EMBEDDER`, `OLLAMA_URL`, `OLLAMA_EMBEDDING_MODEL`, the three mothership vars, `LOG_FORMAT`, `LOG_LEVEL`; (10) **Troubleshooting** — the five most likely stumbles (unset `ADMIN_TOKEN` interpolation fail, cold-Ollama pull, `upstream_not_configured` in air-gap, LAN-browser 403 on admin UI, port conflict); (11) **Implementation status** — kept the existing T-2.1 → T-3.8 checklist as the honest state-of-the-code section (renamed from "Status" to signal it's implementation notes, not user-facing). Registration flow: pointed at `skillsregistry.net` for tenant + API-key sign-up per plan hint; deliberately did not invent a specific self-serve UI flow since the mothership doesn't yet have one and inventing one would drift from reality.
- ☑ **T-4.2** CI + release workflows in `.github/workflows/`: (a) `ci.yml` — on PR + push to `main`: `pnpm install --frozen-lockfile` → `pnpm build` → `pnpm typecheck` → `pnpm test`, plus a `changeset-check` job on PRs that runs `pnpm changeset status --since=origin/main` to force a changeset on any `packages/*` diff; (b) `publish-sdk.yml` — on push to `main`: standard `changesets/action@v1` release-PR flow that npm-publishes any bumped `@skillsregistry/*` package via `NPM_TOKEN`; (c) `publish-app.yml` — on `v*` tag push: `docker/build-push-action@v6` build of `apps/local/Dockerfile` with GHA cache, pushed to `ghcr.io/cogniumhq/skillsregistry-local:<version>` + `:latest` via `GITHUB_TOKEN` (`packages: write`), OCI labels set (source, version, license). Fixed a CI order bug found during audit: the original `ci.yml` ran `typecheck` before `build`, which fails on a pristine CI checkout because `packages/mcp` type-resolves `@skillsregistry/domain` via that package's `dist/*.d.ts` (via `main`/`types` in package.json), and `dist/` doesn't exist until the build runs. Reordered to build → typecheck → test with an inline comment explaining why. **v1.0.0 released**: SDK side — `@skillsregistry/{contracts,domain,eval,mcp}@1.0.1` + `@skillsregistry/{schema,dag}@1.0.0` on npm (contracts patch shipped `AdminSkillsListResponseSchema` for T-3.4/T-3.6; domain/eval/mcp got transitive patch bumps via changesets `updateInternalDependencies: patch`); published manually with OTP (`pnpm release -- --otp <code>`) because the repo has no `NPM_TOKEN` secret configured (org policy also blocks the changesets action from creating the release PR — deferred as a follow-up). App side — `v1.0.0` git tag on commit `47d1f70` triggered `publish-app.yml` run 28720513014 which pushed `ghcr.io/cogniumhq/skillsregistry-local:1.0.0` + `:latest` (both at manifest digest `sha256:9e220570e629b5237dec56cda8c5c59d97f891da1a5f64f7a18034157c60e407`). **arm64 dropped for 1.0.0**: QEMU-emulated `pnpm install --frozen-lockfile` on `ubuntu-latest` blew past the 30min job timeout on the first attempt; cut to `linux/amd64` only for the initial release with an inline comment tracking arm64 as a follow-up (self-hosted arm64 runner or a `pnpm fetch` layer cache).
- ☑ **T-4.3** Verified all five `@skillsregistry/*` packages are live on the public npm registry at `1.0.0` — `contracts`, `schema`, `domain`, `mcp`, `eval`. Registry probe: `npm view <pkg>@1.0.0` returns `version 1.0.0`, valid `dist.tarball`, `main: ./dist/index.js` for every package. Consumer-side smoke: fresh `npm init -y` outside the workspace at `/tmp/sr-consumer-smoke/`, `npm install` all five packages from the registry (no workspace protocol), `node smoke.mjs` imports each with wildcard, asserts non-zero exports, and resolves one live callable per package to catch tree-shaking regressions: `contracts.HealthResponseSchema.parse`, `schema.SkillManifestSchema.parse`, `domain.CircuitBreaker`, `mcp.McpError`, `eval.buildEvalResult`. Exports counts: contracts 45, schema 6, domain 43, mcp 24, eval 11 — all non-zero, all import cleanly under ESM.
- ☑ **T-4.4** Mothership repo audit (read-only per sacred boundary). Result: **mothership correctly declares all five `@skillsregistry/*@^1.0.0` deps in root `package.json` and imports from them at 26 sites across `src/`**, including the hot boot path in `src/components.ts` (which imports `PgVectorProvider`, `ConfidenceGate`, `CircuitBreaker` from `@skillsregistry/domain`). BUT the mothership still has on-disk inlined copies of extracted modules, some of them still live: (a) **live shadow** — `src/providers/pgvector-provider.ts` is still imported by `src/index.ts:9` and constructed at three sites (`:1421`, `:2516`, `:2818`), while `src/components.ts:12` imports `PgVectorProvider` from the SDK; two `PgVectorProvider` classes now coexist and future divergence risk is real; (b) **live shadow** — `src/composition/schema` is still imported by `src/routes/composition.ts:29`, while `src/mcp/routes.ts:15` uses the SDK-exported `getCompositionBySlug` from `@skillsregistry/domain`; (c) **orphaned dupe** — `src/intelligence/` (5 files: composition-detector, confidence-gate, deep-search, reranker-backend, reranker) has zero external importers; superseded by `@skillsregistry/domain` on the live path; (d) **orphaned dupe** — `src/resilience/circuit-breaker.ts` + `.test.ts` is only referenced by the orphaned `src/intelligence/*` cluster. Coordination ask handed off to the mothership session, which executed the cleanup and pushed to `cogniumhq/skillsregistry` `main`: (1) rewrote `src/index.ts` + `src/routes/composition.ts` imports to source `PgVectorProvider` and composition schema from `@skillsregistry/domain`, (2) deleted `src/providers/pgvector-provider.ts`, `src/composition/schema.ts`, `src/intelligence/*`, `src/resilience/circuit-breaker.ts` + its test, (3) `pnpm install` refreshed the lockfile onto the `1.0.1` patch line. Mothership is now single-source-of-truth via the SDK — no live shadows, no orphaned dupes.
- ☐ **T-4.5** Announcement + public preview — coordinated with marketing (out of scope of this repo but tracked)

## Cross-cutting / continuously open

- ◐ **T-C.1** Eval R@5 stays ≥ 75% on local endpoint (5-point regression budget from mothership baseline). **First-run finding**: the gate is well-defined but currently **unrunnable against a fresh `docker compose up`** — the local node ships with an empty catalog (`SELECT COUNT(*) FROM skills` = 0 immediately after boot), and `@skillsregistry/eval` fixtures target 40 specific skill UUIDs (`550e8400-...-446655440001` … `-446655440040`, one per canonical tool: `cargo-deny`, `prettier`, `eslint`, `trivy`, etc.). Ran `node packages/eval/dist/cli.js --endpoint http://localhost:3000/v1/search --limit 5` against a freshly-provisioned local stack: R@1 = 0.0%, R@5 = 0.0%, MRR = 0.000, 91/91 fixtures miss. That's not a regression — it's an absence of seed data. Real remediation for the gate: (a) publish a `@skillsregistry/eval-fixtures` package containing the 40 seed manifests (one `PublishRequest` payload per fixture UUID), (b) add `pnpm --filter @skillsregistry/local seed:eval` that POSTs each to `/v1/skills`, (c) then run eval and land on ≥ 75%. Until (a)+(b), the T-C.1 gate applies only to *populated* endpoints — mothership, staging, or a manually-seeded local. **Not a P0 for the 1.0.x release** since the local node's primary use case is "operator seeds their own private catalog"; the eval gate is developer-facing regression tooling, not an end-user surface.
- ☐ **T-C.2** Every `packages/*` PR carries a changeset — CI enforces
- ◐ **T-C.3** Every route in `apps/local` has a Zod contract in `@skillsregistry/contracts`. **Audit result (post-1.0.1)**: 14 routes surveyed; **13/14 have a matching schema** in contracts (all except `GET /v1/admin/health` which is intentionally an internal deep-probe shape). **Two live drift risks flagged**: (a) `GET /v1/health` emits a local `HealthPayload` interface (`status`/`nodeEnv`/`upstreamConfigured`/`embedder`/`dbReachable`) that is structurally incompatible with `HealthResponseSchema` in contracts (`ok`/`service`/`dbStatus`/`dbLatencyMs`/`tables`/`aiStatus`/etc — the mothership shape); either the local emit should reshape to match the ecosystem contract or contracts needs a `LocalHealthResponseSchema` fork — design decision, tracked as a follow-up. (b) `GET /v1/admin/skills` was locally redeclaring `AdminSkillsListResponseBody` + `AdminSkillsListItem` instead of importing the identical `AdminSkillsListResponse` / `AdminSkillsListItem` types shipped in `@skillsregistry/contracts@1.0.1` for exactly this route — **fixed in commit `a1b061e`** by importing the canonical types from contracts and deleting the local dupes; 384/384 tests stay green. **Runtime response validation is 0/14** — no route wraps its outbound body in `.parse()`; contracts are used for input validation (`POST /v1/skills`, `POST /v1/trust/score` via `PublishRequestSchema` / `TrustScoreRequestSchema`) but never for output. Defense-in-depth `.parse()` on responses is a future hardening, not spec-mandated.
- ☑ **T-C.4** Every upstream call site goes through `upstream-client` — no fetches to `api.skillsregistry.net` elsewhere in `apps/local`. **Audit result**: swept every non-test file under `apps/local/src/` for `fetch(`, `https?://`, and `skillsregistry\.net`. Only **one live `fetch(` invocation** in the entire tree: `apps/local/src/upstream-client/client.ts:285` inside `doTransport(...)`, gated behind the air-gap check → token bucket → circuit breaker → 30s AbortController. `apps/local/src/migration/publish-to-mothership.ts` (the natural boundary-drift risk) delegates through `upstream.publish(request)` and never touches the network directly. `apps/local/src/services.ts:98` mentions the mothership URL only in a docstring; `apps/local/src/config.ts` mentions it only in env-var comments + as an env parse target; `apps/local/src/http/upstream-response.ts:26` mentions it only in a JSDoc. Zero `https://` URL construction outside upstream-client. Sacred boundary holds.

## Deferred to post-MVP

Not tracked as tasks — recorded so future planners see the boundary:

- Multi-tenant local install
- Local Circle-IR scan (all scoring goes through mothership)
- Local sync workers (mothership crawls; local doesn't)
- Enterprise SSO / SAML in admin UI
- Publisher signing PKI local key-management UI
- Bulk migration door (`/v1/migrate/publish-all`)
- Cross-region replication of local install
- Backwards-compat with pre-`1.0.0` SDK versions

---

*Update this file as work lands. Move completed items above the phase-4 fold to keep the file scannable.*
